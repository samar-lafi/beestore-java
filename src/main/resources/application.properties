# Nom de l’application
spring.application.name=beestore

# Profil actif par défaut
spring.profiles.active=dev

# Port du serveur
server.port=8080

# Swagger / OpenAPI
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true

# JPA : afficher les requêtes SQL dans la console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.com.beedigital=DEBUG
logging.level.org.springframework.web=INFO
# Expose uniquement l’endpoint de santé (pas toute la surface Actuator)
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always